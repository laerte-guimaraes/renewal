<div class="alert alert-danger" role="alert">
  <strong> <%= I18n.t(
    'activerecord.errors.messages.not_saved',
    resource: resource.class.model_name.human.downcase
  ) %> </strong>
  <% resource.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
</div>
